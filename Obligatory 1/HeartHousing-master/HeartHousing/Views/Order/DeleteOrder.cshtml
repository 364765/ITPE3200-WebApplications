@model Order 

<h2>Delete</h2>

<h3>Are you sure you want to delete this order?</h3>

<div>
    <dl class="row">
        <dt class="col-sm-2">Order ID</dt>
        <dd class="col-sm-10">@Model.RentalID</dd>

        <dt class="col-sm-2">Total Price</dt>
        <dd class="col-sm-10">@Model.TotalPrice</dd>

        <dt class="col-sm-2">Rental Id</dt>
        <dd class="col-sm-10">@Model.RentalID</dd>

        <dt class="col-sm-2">NigthsNr</dt>
        <dd class="col-sm-10">@Model.NightsNr</dd>

    </dl>

    <form asp-action="DeleteOrderConfirmed" asp-route-id="@Model.OrderID" method="post">
        <button type="submit" class="btn btn-danger">Delete Order</button>
        <a asp-action="Table" class="btn btn-secondary">Cancel</a>
    </form>
</div>